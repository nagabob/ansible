- name: install nginx
  hosts: mongodb
  tasks: 
  - name: install nginx
    ansible.builtin.yum:
      name: nginx
      state: installed
  - name: Start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      

